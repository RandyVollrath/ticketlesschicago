# Parking Quality Diagnosis — 2026-05-11 01:10

**Window:** last 24h · **Rows:** N/A (data unavailable) · **Users:** N/A · **Healthy:** N/A

## Run status: data unavailable

This autonomous improver run completed all setup steps but could not fetch live data. Two paths were tried and both failed:

1. **Admin HTTP endpoint** — `GET https://www.autopilotamerica.com/api/admin/parking-quality-latest?hours=24` was blocked by the sandbox's outbound network allowlist (`Host not in allowlist`). The endpoint itself is healthy; the restriction is agent-side.
2. **Local diagnosis script** — `scripts/parking-quality-diagnose.ts` requires both `.env.local` (Supabase credentials) and `node_modules` to be present. Neither was available in this cloned sandbox.

## What would have happened if data were present

The classifier in `lib/parking-quality-diagnose.ts` ranks rows by this precedence:

```
out_of_coverage → user_said_street_wrong → user_said_side_wrong
→ no_snap → snap_far → nominatim_overrode → walkaway_guard_fired
→ autolabel_disagreed → heading_stale → compass_missing
→ low_accuracy → parity_forced → healthy
```

The top 1–2 signatures from `top_signatures[]` would have been inspected, and a targeted patch to one of these files would have been proposed or implemented:

| Signature | Primary file |
|---|---|
| `no_snap` | `pages/api/mobile/check-parking.ts` (snap logic) |
| `snap_far` | same — distance threshold + candidate selection |
| `nominatim_overrode` | `pages/api/mobile/check-parking.ts` ~lines 1901–2069 (heading-agreement section) |
| `heading_stale` | `TicketlessChicagoMobile/src/services/BackgroundTaskService.ts` |
| `parity_forced` | `lib/chicago-grid-estimator.ts` |
| `walkaway_guard_fired` | `pages/api/mobile/check-parking.ts` ~line 2900 |

## Proposed patches

None — evidence threshold not met (requires ≥3 example rows or ≥1 `user_said_street_wrong` row per skill hard rules).

## Infrastructure fix needed

To unblock future autonomous runs, one of the following is required:

**Option A — Allow outbound HTTPS to autopilotamerica.com in the agent sandbox.**  
The endpoint `/api/admin/parking-quality-latest` already exists and is authenticated with `CRON_SECRET`. This is the cleanest path.

**Option B — Populate `.env.local` in the sandbox at clone time.**  
If `NEXT_PUBLIC_SUPABASE_URL` and `SUPABASE_SERVICE_ROLE_KEY` are available, the local `scripts/parking-quality-diagnose.ts` script can run without any outbound HTTP to autopilotamerica.com (it talks directly to Supabase). Also requires `npm install` to be run as part of sandbox setup so `tsx` is available.

## Next steps

No code changes in this PR. Once the infrastructure fix is in place, re-trigger the agent — it will fetch live `top_signatures`, inspect example rows, and propose a concrete patch.
