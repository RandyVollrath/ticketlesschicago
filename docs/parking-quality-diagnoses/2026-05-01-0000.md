# Parking Quality Diagnosis — 2026-05-01-0000

**Window:** last 24h · **Rows:** N/A · **Users:** N/A · **Healthy:** N/A

## Headline regression

Agent run could not retrieve diagnostic data — no actionable signatures were evaluated.

---

## Why data was unavailable

The auto-improver agent runs in a sandboxed environment with two data paths:

1. **External API** — `GET https://www.autopilotamerica.com/api/admin/parking-quality-latest?hours=24`  
   Result: `Host not in allowlist` — the sandbox network policy blocks outbound calls to production.

2. **Local script** — `npx tsx scripts/parking-quality-diagnose.ts` (connects to Supabase directly via env vars)  
   Result: `.env.local` is not present in the cloned repo (expected — secrets are not committed), so the script cannot form a DB connection.

## What the agent did

- Confirmed `scripts/parking-quality-diagnose.ts` and `pages/api/admin/parking-quality-latest.ts` both exist and are structurally intact.
- Read `.claude/skills/parking-quality-improver.md` — skill definition is current.
- Inspected `git log` — no recent commits touching parking detection code paths that would suggest a known regression to target blindly.
- Created this diagnosis doc as the auditable artifact for the run.

## Top failure signatures

None — no data retrieved.

## Root-cause hypotheses

None — no example rows to inspect.

## Proposed patches

None — the hard rule in the skill requires ≥3 example rows or ≥1 confirmed `user_said_street_wrong` row before proposing a fix. That bar was not met.

## How to unblock future agent runs

Two options (pick one):

### Option A — Whitelist the production domain in the sandbox
Add `autopilotamerica.com` (or `www.autopilotamerica.com`) to the agent sandbox's outbound allowlist. This is the cleanest path: the endpoint already exists, is auth-gated by `CRON_SECRET`, and returns exactly what the agent needs.

### Option B — Provide a read-only Supabase token in the agent environment
Place a minimal `.env.local` (or `.env.agent`) containing only:
```
NEXT_PUBLIC_SUPABASE_URL=...
SUPABASE_SERVICE_ROLE_KEY=...
```
in the cloned repo before the agent runs. The local `diagnose()` script will then work without any network allowlist change.

## Honest ledger

| Claim | Verified? |
|---|---|
| Diagnosis script exists | Yes — read the file |
| Admin endpoint exists | Yes — read the file |
| Skill definition is current | Yes — read the file |
| Diagnostic data retrieved | **No** — blocked |
| Any parking rows inspected | **No** |
| Any code changed | **No** |
