# Parking Quality Diagnosis — 2026-04-29 1200

**Window:** last 24h · **Rows:** unavailable · **Users:** unavailable · **Healthy:** unavailable

## Run status

This automated improver run could not retrieve a quality report. Both data paths were blocked:

1. **Admin endpoint blocked.** `GET https://www.autopilotamerica.com/api/admin/parking-quality-latest?hours=24` returned `Host not in allowlist` — the sandbox environment this agent runs in does not allow outbound HTTP to the production host.

2. **Local diagnosis script failed.** `scripts/parking-quality-diagnose.ts` requires `NEXT_PUBLIC_SUPABASE_URL` and `SUPABASE_SERVICE_ROLE_KEY` environment variables. No `.env.local` file is present in the clone (correct — credentials are not checked in).

## What was attempted

```
curl -s -H 'Authorization: Bearer <CRON_SECRET>' \
  'https://www.autopilotamerica.com/api/admin/parking-quality-latest?hours=24'
# → Host not in allowlist

npx tsx scripts/parking-quality-diagnose.ts hours=24
# → Error: supabaseUrl is required.
```

## No code changes

Per the hard rules in `.claude/skills/parking-quality-improver.md`, no code changes are proposed without at least 3 supporting example rows or 1 confirmed `user_said_street_wrong` row. No data was available to support any proposal.

## Headline regression

None assessed — data unavailable.

## Top failure signatures

None assessed — data unavailable.

## Root-cause hypotheses

None — no rows to inspect.

## Proposed patches

None — insufficient evidence.

## Action required from human reviewer

To unblock future automated improver runs, one of the following must be true:

### Option A — Allow outbound HTTP in the agent sandbox
Grant the agent's execution environment network access to `www.autopilotamerica.com`. The endpoint already requires the `CRON_SECRET` bearer token, so auth is not a concern.

### Option B — Inject a snapshot file into the clone
Before invoking the agent, write the latest quality report to a file in the repo (e.g. `tmp/parking-quality-latest.json`) and have the agent read it locally rather than curling the live endpoint.

### Option C — Provide an `.env.local` at runtime
Inject `NEXT_PUBLIC_SUPABASE_URL` and `SUPABASE_SERVICE_ROLE_KEY` as ephemeral environment variables at the start of the agent session so the local `diagnose()` script can connect directly.

## Notes on last shipped improvements

The three most recent quality-related commits before this run were:

- `6885313` — Skip Mapbox map-matching on three-way snap agreement (timeout fix)
- `226a4b6` — Fix snap_far false positives + #79 wrong-street pick + classifier signal
- `3697a68` — Android: add Activity Recognition as fallback parking signal

These are worth monitoring in the next successful report run for any regression in `snap_far` or `nominatim_overrode` counts.
