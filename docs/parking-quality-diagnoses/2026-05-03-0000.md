# Parking Quality Diagnosis — 2026-05-03-0000

**Window:** last 24h · **Run by:** auto-improver agent · **Status:** DATA UNAVAILABLE

## Why this doc exists without data

The auto-improver agent ran at its scheduled time (30 min after the daily quality cron)
but could not retrieve the quality report via either available path:

1. **External API blocked**: `GET https://www.autopilotamerica.com/api/admin/parking-quality-latest?hours=24`
   returned `Host not in allowlist` — the Claude Code sandbox network policy does not permit
   outbound requests to `autopilotamerica.com` from the agent runner environment.

2. **Local script unavailable**: `.env.local` is not present in the cloned repo, so
   `scripts/parking-quality-diagnose.ts` cannot connect to Supabase directly.

## Impact

No failure signatures could be identified. No code changes were made.
This run is a no-op from a quality-improvement perspective.

## Recommended remediation

To unblock future automated runs, one of the following is needed:

### Option A — Inject credentials at run time (preferred)
Populate `.env.local` (or environment variables) in the agent runner with
`NEXT_PUBLIC_SUPABASE_URL` and `SUPABASE_SERVICE_ROLE_KEY` before spawning the agent.
The local `diagnose()` function will then work without any network egress.

### Option B — Allow-list the domain in the sandbox policy
Add `autopilotamerica.com` to the Claude Code network allowlist so the external
admin endpoint can be called. The endpoint already requires `CRON_SECRET` Bearer auth,
so the exposure is minimal.

### Option C — Emit a report artifact during the daily cron
Have `pages/api/cron/parking-quality-daily.ts` write a JSON file to the repo
(or a well-known S3/R2 path) that the agent can read without any live DB access.

## Proposed patches

None — no data, no evidence base. The hard rule is ≥3 example rows per signature
before proposing a fix.

## Next steps for human reviewer

1. Pick Option A, B, or C above and implement.
2. Re-run the auto-improver agent after credentials/access are available.
3. If this is the third consecutive no-data run, treat it as a monitoring gap
   (the daily cron may itself be failing) and check the cron job logs.
