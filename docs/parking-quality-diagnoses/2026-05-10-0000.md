# Parking Quality Diagnosis — 2026-05-10-0000

**Window:** last 24h · **Rows:** N/A · **Users:** N/A · **Healthy:** N/A

## Run outcome: report unavailable

The auto-improver agent could not fetch the quality report. The admin endpoint
(`GET /api/admin/parking-quality-latest?hours=24`) requires a `Bearer` token
that cannot be passed from this sandbox environment:

- **Bash tool**: blocked from reaching external hostnames (sandbox allowlist
  restriction — `Host not in allowlist` error on `curl` to
  `autopilotamerica.com`).
- **WebFetch tool**: capable of reaching external URLs but cannot attach custom
  `Authorization` headers, so the endpoint returned HTTP 403.
- No GitHub MCP tool provides a general authenticated HTTP fetch capability.

Because there is **no live data**, the skill's hard rule applies:

> *"Skip proposing fixes that aren't supported by ≥3 example rows or ≥1
> confirmed user-feedback-wrong row."*

No code changes are proposed in this run.

## What was read

Despite the data gap, the following code paths were read to stay current:

| File | Purpose |
|------|---------|
| `lib/parking-quality-diagnose.ts:115` | `classify()` — signature precedence order |
| `pages/api/admin/parking-quality-latest.ts` | Report endpoint — calls `diagnose()` |
| `pages/api/mobile/check-parking.ts:482–750` | Heading selection, snap pipeline, CarPlay truncation |

The classifier precedence (as of this run) is:

```
out_of_coverage → user_said_street_wrong → user_said_side_wrong →
no_snap → snap_far (>20m, unconfirmed by Mapbox) → nominatim_overrode →
walkaway_guard_fired → autolabel_disagreed → heading_stale →
compass_missing → low_accuracy → parity_forced → healthy
```

Recent commits that changed relevant code paths:

- `7fcf483` — Parking detection: compass heading can contradict Nominatim override
- `96e2d28` — Parking detection: walk-away guard + low-confidence suppression
- `ee1df7f` — Parking accuracy: regression replay suite + replay_inputs persistence

## Proposed fixes

None. Insufficient data to support any patch (hard rule: ≥3 example rows
required).

## Next step for the human reviewer

The sandbox needs one of:

1. **Allowlist `autopilotamerica.com`** in the agent's Bash sandbox so `curl`
   can reach the endpoint with the bearer token. This is the simplest fix.
2. **Add a fetch-with-headers MCP tool** (or a local proxy script that
   writes the report JSON to a temp file before the agent starts) so the
   agent can read the fresh report without making an external request itself.
3. **Pre-fetch the report** in the cron that invokes this agent and pass it as
   a file argument, e.g. `QUALITY_REPORT_PATH=/tmp/quality-report.json`.

Any of the three approaches would let the next run complete the full workflow.

## Honest ledger

- Verified live: **nothing** — no real endpoint reached.
- Verified by code read: classifier precedence in `lib/parking-quality-diagnose.ts`.
- Compiler / type-check: not run (no code changes).
