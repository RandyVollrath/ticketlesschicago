# Parking Quality Diagnosis — 2026-05-04-1337

**Window:** last 24h · **Rows:** N/A · **Users:** N/A · **Healthy:** N/A

## Agent run result: data unavailable

This diagnosis doc was written by the autonomous parking-quality-improver agent. The agent completed its startup checks but **could not retrieve any parking diagnostic data** — neither from the live admin endpoint nor from the local diagnose script. No failure signatures could be identified, so no code changes were made.

This is NOT a clean-report exit (which would mean the report ran and found nothing). This is an infrastructure access failure — the report itself was never retrieved.

---

## Why data was unavailable

### Attempt 1 — Admin endpoint via curl
```
curl -H 'Authorization: Bearer <token>' 'https://www.autopilotamerica.com/api/admin/parking-quality-latest?hours=24'
```
Result: **Host not in allowlist** — the agent sandbox's Bash tool blocks outbound HTTP to external hosts.

### Attempt 2 — Admin endpoint via WebFetch
WebFetch attempted the same URL with the auth header. Result: **HTTP 403** — the WebFetch tool cannot pass custom request headers for authentication.

### Attempt 3 — Local diagnose script
```bash
node -r dotenv/config node_modules/.bin/tsx scripts/parking-quality-diagnose.ts dotenv_config_path=.env.local hours=24
```
Result: **`supabaseUrl is required`** — `.env.local` is absent from the cloned repo (correctly excluded from version control), so the script has no Supabase credentials and cannot query `parking_diagnostics`.

---

## Top failure signatures

None identified — no data retrieved.

---

## Root-cause hypotheses

None — no example rows to analyze.

Per the skill's hard rule: *"Skip proposing fixes that aren't supported by ≥3 example rows or ≥1 confirmed user-feedback-wrong row. Low-signal guesses are worse than nothing."*

---

## Proposed patches

None this run. No code changes committed.

---

## What needs to change for future agent runs

The agent needs exactly one of these to succeed. In priority order:

### Option A — Inject DB credentials at agent launch (recommended)
Set `NEXT_PUBLIC_SUPABASE_URL` and `SUPABASE_SERVICE_ROLE_KEY` (or `NEXT_PUBLIC_SUPABASE_ANON_KEY`) as environment variables in the agent's execution environment. This lets the local diagnose script connect to Supabase without needing `.env.local` on disk.

### Option B — Allowlist the admin endpoint in the sandbox
Add `autopilotamerica.com` (or a local proxy URL) to the Bash tool's network allowlist. This lets the agent call the `/api/admin/parking-quality-latest` endpoint directly with the bearer token.

### Option C — Pre-run data export
Have the daily diagnosis cron write its JSON output to a file in the repo (e.g. `tmp/parking-quality-latest.json`) before the agent run. The agent reads that file instead of making an HTTP request. Downside: sensitive row data in the repo unless the file is gitignored and injected separately.

---

## Honest ledger

| Step | Result |
|---|---|
| Admin endpoint (curl) | BLOCKED — host not in allowlist |
| Admin endpoint (WebFetch) | FAILED — 403, no auth header support |
| Local diagnose script | FAILED — supabaseUrl missing |
| Failure signatures identified | 0 |
| Code changes made | 0 |
| Diagnosis doc | Written (this file) |
| Draft PR | Opened |
