# Parking Quality Diagnosis — 2026-05-11 13:02

**Window:** last 24h · **Agent run:** 2026-05-11T13:02Z · **Data retrieved:** NONE (see below)

## Run outcome: blocked endpoint

This automated improver run could not retrieve parking diagnostic data. The admin endpoint
(`GET /api/admin/parking-quality-latest?hours=24`) returned `Host not in allowlist` when called
from the autonomous agent's execution environment. The network sandbox blocks outbound HTTP to
production domains.

**No parking data was analyzed. No code changes were made.**

## What this is NOT

This is not a sign that the parking-quality pipeline is unhealthy. It is an infrastructure
limitation of the agent's sandboxed network environment — the same restriction that blocks `curl`
to any external host.

## Recommended fixes (pick one)

### Option A — Allow the endpoint via environment variable (preferred)
Pass `CRON_SECRET` and `NEXT_PUBLIC_SUPABASE_URL` / `SUPABASE_SERVICE_ROLE_KEY` into the agent's
environment so it can run `scripts/parking-quality-diagnose.ts` locally against Supabase directly,
without needing an outbound HTTP call to the production domain.

```bash
# In the agent's environment or .env.local:
NEXT_PUBLIC_SUPABASE_URL=https://dzhqolbhuqdcpngdayuq.supabase.co
SUPABASE_SERVICE_ROLE_KEY=<key>
```

The diagnose script (`scripts/parking-quality-diagnose.ts`) talks to Supabase directly — it does
not go through the Next.js app server, so it would work even when `autopilotamerica.com` is
blocklist-blocked.

### Option B — Run the diagnose script as a pre-step outside the sandbox
Run `node -r dotenv/config node_modules/.bin/tsx scripts/parking-quality-diagnose.ts dotenv_config_path=.env.local hours=24 > /tmp/parking-quality-report.json`
before launching the agent, then pass the path to the output file as context.

### Option C — Whitelist the production domain in the agent's network policy
Allow `www.autopilotamerica.com` in the outbound allowlist so the `curl` call in Step 1 can
complete. This is the most direct fix but requires a network policy change in the hosting
environment.

## What happens on the next run

If the infrastructure issue is resolved before the next scheduled run (either by providing
DB credentials or by whitelisting the domain), the agent will execute the full workflow:
pull the report, identify the top failure signature, inspect example rows, write a real
diagnosis, and implement a targeted patch.

## Signature reference (no-data baseline)

For reviewer context, the failure signatures the agent would have scored are:

| Signature | What it means | Implicated code |
|---|---|---|
| `no_snap` | No street matched at all | `pages/api/mobile/check-parking.ts` |
| `snap_far` | Snapped to street >20m away | Same — distance threshold |
| `nominatim_overrode` | Nominatim contradicted snap | Heading-agreement section |
| `heading_stale` | GPS heading from before a turn | `BackgroundTaskService.ts` |
| `compass_missing` | No compass heading captured | Check-parking stale guard |
| `user_said_street_wrong` | Ground truth: user flagged wrong street | Full decision chain |
| `parity_forced` | House-number parity override fired | `lib/chicago-grid-estimator.ts` |
| `autolabel_disagreed` | Post-departure snap disagreed | `lib/parking-quality-diagnose.ts` classify() |

## Proposed patches

None — insufficient data to propose any patch. See Hard Rule #4 in
`.claude/skills/parking-quality-improver.md`: no fixes without ≥3 example rows or ≥1
confirmed `user_said_street_wrong` row.

---
*Generated by the auto parking-quality-improver agent on 2026-05-11T13:02Z.*
*Run status: BLOCKED (no data). No code changes in this PR.*
