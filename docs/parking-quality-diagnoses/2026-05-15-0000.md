# Parking Quality Diagnosis — 2026-05-15 00:00

**Window:** last 24h · **Rows:** N/A · **Users:** N/A · **Healthy:** N/A

## Run outcome: data access blocked

The auto-improver agent ran but could not retrieve the quality report.

```
GET /api/admin/parking-quality-latest?hours=24
HTTP 403 — "Host not in allowlist"
```

The Vercel project has an IP allowlist enabled (`"github": { "deploymentProtection": true }`
in `vercel.json`, plus a separate IP allowlist configured in the Vercel dashboard). The agent's
egress IP is not on that allowlist. The local fallback — running
`scripts/parking-quality-diagnose.ts` directly — was also unavailable because `.env.local`
is not present in this environment.

## Headline regression

Cannot determine — no data retrieved this run.

## Top failure signatures

None identified — diagnosis aborted at data-retrieval step.

## Root-cause hypotheses

N/A

## Proposed patches

None. The skill's hard rule prohibits proposing fixes without ≥3 example rows or ≥1 confirmed
`user-feedback-wrong` row.

## Action required to unblock future runs

Two options, either suffices:

### Option A — Add agent egress IPs to the Vercel allowlist
1. Identify the static egress IP(s) used by this agent's execution environment.
2. In Vercel dashboard → Project → Settings → Deployment Protection → IP Allowlist, add those
   IPs (or CIDR range).
3. The endpoint `/api/admin/parking-quality-latest` will then be reachable without code changes.

### Option B — Supply `.env.local` in the agent environment
1. Add `NEXT_PUBLIC_SUPABASE_URL` and `SUPABASE_SERVICE_ROLE_KEY` to the agent's environment
   (not committed — injected at runtime via secrets manager or CI secret).
2. The agent can then call `scripts/parking-quality-diagnose.ts` directly, bypassing the HTTP
   endpoint entirely.

Option B is lower-friction: it avoids IP management and lets the agent query Supabase directly
with service-role credentials.

## Verified live

Nothing — no data was retrieved, no code was changed, no smoke test was run.
This PR exists solely to document that the agent ran and what it found.
