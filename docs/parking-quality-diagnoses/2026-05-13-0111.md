# Parking Quality Diagnosis — 2026-05-13-0111

**Window:** last 24h · **Rows:** N/A (data unavailable) · **Users:** N/A · **Healthy:** N/A

## Run status: DATA UNAVAILABLE — infrastructure issue

This autonomous improver run could not retrieve live data. Two blockers:

1. **Network sandbox blocked the admin endpoint.** The curl to
   `https://www.autopilotamerica.com/api/admin/parking-quality-latest?hours=24`
   returned `Host not in allowlist`. The execution environment's network sandbox
   does not permit outbound connections to `www.autopilotamerica.com`.

2. **Supabase credentials not present.** Neither `NEXT_PUBLIC_SUPABASE_URL` nor
   `SUPABASE_SERVICE_ROLE_KEY` are set in the environment, so the fallback of
   running the local `scripts/parking-quality-diagnose.ts` directly also fails.

Because the skill's hard rule #4 prohibits proposing fixes without ≥3 example
rows, **no code changes are included in this PR.**

## What needs to be fixed before the next run

For this autonomous agent to operate as designed, the runner environment needs
**one** of the following:

### Option A — Allow network access to the live admin endpoint (preferred)

Add `www.autopilotamerica.com` to the Bash tool's network allowlist in
`.claude/settings.json` (or in the runner's launch configuration). The agent
then curls the existing `GET /api/admin/parking-quality-latest?hours=24`
endpoint with the Bearer token and proceeds normally. No code changes needed.

### Option B — Inject Supabase credentials as environment variables

Set `NEXT_PUBLIC_SUPABASE_URL` and `SUPABASE_SERVICE_ROLE_KEY` in the runner's
environment (e.g. GitHub Actions secrets wired to the job). The agent would then
bypass the HTTP call and query Supabase directly via the local
`scripts/parking-quality-diagnose.ts` script.

## Proposed patches

None. The data-evidence requirement in the improver skill cannot be satisfied
without actual diagnostic rows. Adding hypothetical patches without row-level
evidence would violate the skill's hard rules.

## Next steps for the human reviewer

1. Decide between Option A and Option B above.
2. Wire the chosen credential/allowlist fix to the runner (GitHub Actions job or
   Claude Code web session config).
3. Re-run the improver — it will pick up from Step 1 with real data.

## Code paths reviewed (static, no live data)

While waiting for data, the following files were confirmed to exist and were
read to understand the pipeline shape (no findings without row evidence):

- `pages/api/mobile/check-parking.ts` — main mobile parking check handler
- `lib/parking-quality-diagnose.ts` — classification logic (`classify()` fn,
  lines 115–141), failure signature definitions
- `pages/api/admin/parking-quality-latest.ts` — the blocked admin endpoint
- `.claude/skills/parking-quality-improver.md` — skill workflow and hard rules

No anomalies were found in the classifier logic itself on a static read. The
`snap_far` guard at line 131 (lib/parking-quality-diagnose.ts) has a nuanced
condition that could merit future review once real data confirms it fires
incorrectly.
